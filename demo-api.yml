server:
  port: 8090

spring:
  boot:
    admin:
      client:
        url: http://localhost:8091

management:
  endpoints:
    web:
      exposure:
        include: "*"

message: "This is test message from config server"
